# stripe-reconciler

A Stripe webhook receiver that implements its own event sequencing and
idempotency logic, plus the test suite that proves the logic works.

The build pipeline lives in `_build/`. **Start at [`_build/CLAUDE.md`](_build/CLAUDE.md).**

Conventions for editing code here: [`_build/_shared/conventions.md`](_build/_shared/conventions.md).

This file is a pointer. It does not accumulate content.
